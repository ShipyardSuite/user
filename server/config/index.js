import DBConfig from "./dbConfig";
import ExpressConfig from "./expressConfig";
import LoggerConfig from "./loggerConfig";

export default { DBConfig, ExpressConfig, LoggerConfig };
export { DBConfig, ExpressConfig, LoggerConfig };