const expect = require('chai').expect;

describe('Hello world', function() {
	describe('boolean test operation', function() {
		it('true should be true', function() {
			expect(true).to.equal(true);
		});
	});
});
